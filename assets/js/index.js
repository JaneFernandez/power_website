// index.js - logique du quiz d'objectif (page index.html)

function choisir(name, id) {
    document.getElementById("hidden-goal-id").value = id;
    document.getElementById("formulaire-email").style.display = "block";
}
